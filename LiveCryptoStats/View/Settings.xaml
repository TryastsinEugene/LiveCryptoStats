<UserControl x:Class="LiveCryptoStats.View.Settings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:vm="clr-namespace:LiveCryptoStats.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Style="{StaticResource Page_Style}">

    <UserControl.DataContext>
        <vm:SettingVM />
    </UserControl.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Settings"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top"
                       Margin="0,20,0,0"/>

        <StackPanel Grid.Row="1" Margin="20" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button 
                    Style="{StaticResource SettingsButton}"
                   >
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Icon:PackIconMaterial Kind="Lightbulb" Style="{StaticResource Settings_Icon_Style}" />
                    <TextBlock Style="{StaticResource Settings_Text_Style}" Text="Light Mode"/>
                </StackPanel>
            </Button>
            <Button Command="{Binding ChangeLanguageCommand}"
                    Style="{StaticResource SettingsButton}"
                     >
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Icon:PackIconMaterial Kind="Translate" Style="{StaticResource Settings_Icon_Style}" />
                    <TextBlock Style="{StaticResource Settings_Text_Style}" Text="Ukrainian"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
