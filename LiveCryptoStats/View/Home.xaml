<UserControl x:Class="LiveCryptoStats.View.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:LiveCryptoStats.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="600"
             d:DesignWidth="622"
             Style="{StaticResource Page_Style}">

    <UserControl.DataContext>
        <vm:HomeVM />
    </UserControl.DataContext>
    
    <Grid>
        
        <DataGrid x:Name="CryptoDataGrid"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  RowBackground="#252538"
                  AlternatingRowBackground="#1E1E2F"
                  Foreground="#252538"
                  GridLinesVisibility="None"
                  Background="#1E1E2F"
                  BorderBrush="#1E1E2F"
                  FontSize="14"
                  Margin="10">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Назва" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Ціна" Binding="{Binding Price}" Width="*"/>
                <DataGridTextColumn Header="24ч" Binding="{Binding Change24h}" Width="*"/>
                <DataGridTextColumn Header="Обʼєм за 24ч" Binding="{Binding Volume24h}" Width="*"/>
                <DataGridTextColumn Header="Капіталізація" Binding="{Binding MarketCap}" Width="*"/>
                <DataGridTemplateColumn Header="Дії" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="🔍" Margin="2" Width="30" Height="30" />
                                <Button Content="🔔" Margin="2" Width="30" Height="30" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
