<UserControl x:Class="LiveCryptoStats.View.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:LiveCryptoStats.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="600"
             d:DesignWidth="822"
             Style="{StaticResource Page_Style}">

    <UserControl.DataContext>
        <vm:HomeVM />
    </UserControl.DataContext>
    
    <Grid>

        <DataGrid x:Name="cboCurrencies"
                  ItemsSource="{Binding Currencies}"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  RowBackground="#252538"
                  AlternatingRowBackground="#1E1E2F"
                  Foreground="#252538"
                  GridLinesVisibility="None"
                  Background="#1E1E2F"
                  BorderBrush="#1E1E2F"
                  FontSize="14">

            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Назва" Width="auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                                <Image Source="{Binding ImageUrl}" Width="30" Height="30" Stretch="Uniform" Margin="0,0,10,0"/>

                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Code}" 
                               Style="{StaticResource Grid_Text_Style}" 
                               />
                                    <TextBlock Text="{Binding Name}" 
                                               Style="{StaticResource Grid_Text_Style}" FontSize="15" 
                                               Margin="4,3,0,0" Foreground="Gray"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Price" Binding="{Binding Price}" ElementStyle="{StaticResource Grid_Text_Style}" Width="*"/>
                <DataGridTextColumn Header="Supply" Binding="{Binding Supply}" ElementStyle="{StaticResource Grid_Text_Style}" Width="*"/>
                <DataGridTextColumn Header="Volum(24hr)" Binding="{Binding Volume}" ElementStyle="{StaticResource Grid_Text_Style}" Width="*"/>
                <DataGridTextColumn Header="Change(24hr)" Binding="{Binding ChangePercent24Hr}" ElementStyle="{StaticResource Grid_Text_Style}" Width="*"/>
                <DataGridTemplateColumn Header="Дії" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="🔍" Margin="2" Width="30" Height="30" />
                                <Button Content="🔔" Margin="2" Width="30" Height="30" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
